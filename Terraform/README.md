# WEBAPP-3-Tier-AWS

Do not store real secrets in `terraform.tfvars`. Use placeholders locally and provide sensitive values through a secure secrets manager, environment variables, or protected CI/CD variables.
